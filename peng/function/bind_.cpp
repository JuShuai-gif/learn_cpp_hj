#include <functional>
#include <vector>
#include <algorithm>
#include <iostream>
/*
class Cloud {
private:
    int arr[10] = {12, 23, 16, 18, 46, 10, 2, 78, 9};

public:
    bool comp(int a, int b) {
        return a > b ? true : false;
    }

    void function() {
        std::sort(arr, arr + 10, comp);
    }

    void print() {
        for (size_t i = 0; i < 10; i++) {
            std::cout << arr[i] << " ";
        }
        std::cout << std::endl;
    }
};
*/

int main() {


    return 0;
}
